// Re-exported from the shared package so the app and the lander render the
// exact same level text. Source: pkg/shared/src/react/quiz/LevelText.tsx
export { LevelText } from "@k53studyguide/shared/react";
